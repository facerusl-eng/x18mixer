<UserControl x:Class="WpfMixer.Views.RoutingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WpfMixer.Views"
             xmlns:models="clr-namespace:WpfMixer.Models"
             mc:Ignorable="d"
             Background="{StaticResource BgDeep}">

    <TabControl Background="{StaticResource BgDeep}"
                BorderBrush="{StaticResource BorderBrush}"
                Foreground="{StaticResource TextSecondary}">

        <TabControl.Resources>
            <Style TargetType="TabItem">
                <Setter Property="Background" Value="{StaticResource BgPanel}" />
                <Setter Property="Foreground" Value="{StaticResource TextSecondary}" />
                <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
                <Setter Property="Padding" Value="14,6" />
                <Setter Property="FontFamily" Value="{StaticResource MonoFont}" />
                <Setter Property="FontSize" Value="11" />
                <Style.Triggers>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="{StaticResource BgStrip}" />
                        <Setter Property="Foreground" Value="{StaticResource Accent}" />
                    </Trigger>
                </Style.Triggers>
            </Style>
        </TabControl.Resources>

        <!-- ═══════════════ TAB 1: CHANNEL ROUTING ═══════════════ -->
        <TabItem Header="CHANNEL ROUTING">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- Section header -->
                <Border Grid.Row="0" Background="{StaticResource BgPanel}"
                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1"
                        Padding="12,6">
                    <TextBlock Text="INPUT SOURCE  ·  BUS SENDS  ·  MAIN LR"
                               Foreground="{StaticResource TextSecondary}"
                               FontFamily="{StaticResource MonoFont}" FontSize="10" />
                </Border>

                <!-- Horizontal scroll of all channel routing panels -->
                <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto"
                              VerticalScrollBarVisibility="Disabled"
                              Background="{StaticResource BgDeep}">
                    <StackPanel Orientation="Horizontal" Margin="8">
                        <!-- Input channels -->
                        <ItemsControl ItemsSource="{Binding Mixer.InputChannels}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type models:Channel}">
                                    <local:ChannelRoutingPanel DataContext="{Binding}"
                                                               Margin="2,0" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- Divider -->
                        <Border Width="2" Background="{StaticResource BorderBrush}" Margin="6,4" />

                        <!-- FX Returns -->
                        <ItemsControl ItemsSource="{Binding Mixer.FxReturns}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type models:Channel}">
                                    <local:ChannelRoutingPanel DataContext="{Binding}"
                                                               Margin="2,0" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- Divider -->
                        <Border Width="2" Background="{StaticResource BorderBrush}" Margin="6,4" />

                        <!-- Main LR -->
                        <local:ChannelRoutingPanel DataContext="{Binding Mixer.MainLR}" Margin="2,0" />
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </TabItem>

        <!-- ═══════════════ TAB 2: OUTPUT ROUTING ═══════════════ -->
        <TabItem Header="OUTPUT ROUTING">
            <local:OutputRoutingGrid DataContext="{Binding}" />
        </TabItem>

        <!-- ═══════════════ TAB 3: USB ROUTING ═══════════════ -->
        <TabItem Header="USB ROUTING">
            <local:UsbRoutingGrid DataContext="{Binding}" />
        </TabItem>

    </TabControl>
</UserControl>
