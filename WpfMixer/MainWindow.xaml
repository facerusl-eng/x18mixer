<Window x:Class="WpfMixer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:WpfMixer.Views"
        xmlns:conv="clr-namespace:WpfMixer.Converters"
        Title="WPF Mixer" Height="780" Width="1320"
        Background="#1a1a2e"
        PreviewKeyDown="Window_PreviewKeyDown"
        PreviewKeyUp="Window_PreviewKeyUp"
        Closing="Window_Closing">

    <Window.Resources>
        <conv:BoolToMuteColorConverter x:Key="MuteColor"/>
        <conv:BoolToMuteTextConverter x:Key="MuteText"/>
        <conv:BoolToVisibilityConverter x:Key="BoolVis"/>

        <Style TargetType="Border" x:Key="DarkPanel">
            <Setter Property="Background" Value="#16213e"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="8"/>
        </Style>

        <Style TargetType="Button" x:Key="ActionButton">
            <Setter Property="Background" Value="#0f3460"/>
            <Setter Property="Foreground" Value="#e0e0e0"/>
            <Setter Property="BorderBrush" Value="#e94560"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#e94560"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Style="{StaticResource DarkPanel}" Margin="0,0,0,6">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="WPF MIXER" FontSize="18" FontWeight="Bold"
                           Foreground="#e94560" VerticalAlignment="Center" Margin="0,0,20,0"/>
                <Button Content="+ Channel" Style="{StaticResource ActionButton}"
                        Command="{Binding AddChannelCommand}" Margin="0,0,6,0"/>
                <Button Content="+ Group" Style="{StaticResource ActionButton}"
                        Command="{Binding AddMuteGroupCommand}" Margin="0,0,6,0"/>
                <Button Content="Keyboard Map" Style="{StaticResource ActionButton}"
                        Click="ShowKeyMap_Click" Margin="0,0,6,0"/>
                <Button Content="Export Profile" Style="{StaticResource ActionButton}"
                        Click="ExportProfile_Click" Margin="0,0,6,0"/>
                <Button Content="Import Profile" Style="{StaticResource ActionButton}"
                        Click="ImportProfile_Click"/>
            </StackPanel>
        </Border>

        <!-- Channel strips -->
        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding Channels}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <views:ChannelStripControl Margin="4,0"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Mute Groups -->
        <Border Grid.Row="2" Style="{StaticResource DarkPanel}" Margin="0,6,0,0">
            <StackPanel>
                <TextBlock Text="MUTE GROUPS" Foreground="#aaa" FontSize="11" Margin="0,0,0,6"/>
                <ItemsControl ItemsSource="{Binding MuteGroups}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <views:MuteGroupButtonControl Margin="4"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <!-- Status bar -->
        <Border Grid.Row="3" Background="#0a0a18" Padding="8,4" Margin="0,4,0,0">
            <Grid>
                <TextBlock Text="{Binding LastKeyAction}" Foreground="#888" FontSize="12"
                           VerticalAlignment="Center"/>
                <Border HorizontalAlignment="Center" VerticalAlignment="Center"
                        Background="#e94560" CornerRadius="6" Padding="14,6"
                        Visibility="{Binding ShowKeyOverlay, Converter={StaticResource BoolVis}}">
                    <TextBlock Text="{Binding OverlayText}" Foreground="White"
                               FontSize="14" FontWeight="Bold"/>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
